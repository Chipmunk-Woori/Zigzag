import React, { useState, useEffect } from "react";
import { Provider } from "react-redux";
import { createStore } from "redux";

import {
  StyleSheet,
  Text,
  View,
  Image,
  TouchableOpacity,
  FlatList,
} from "react-native";

const Store_heartProduct = () => {
  return (
    <View>
      <Text style={{ fontSize: 40 }}>스토어 따라해보는 중</Text>
    </View>
  );
};

export default Store_heartProduct;
